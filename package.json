{
  "name": "api-guardian",
  "version": "0.1.0",
  "description": "HTTP route contract extraction and diff for TypeScript projects (pluggable parsers; NestJS included)",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "bin": {
    "api-guardian": "build/cli.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "start": "node build/cli.js",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "gts": "^7.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "eslint": "^10.1.0",
    "prettier": "^3.8.1",
    "ts-morph": "^27.0.2"
  }
}
